{
  "name": "genrojotree",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:rojo": "node tools/genRojoTree.js",
    "watch:rojo": "chokidar \"src/**/*\" -c \"npm run build:rojo\"",
    "server:rojo": "rojo serve",
    "dev": "npm run build:rojo && concurrently -n watch,rojo -c blue,green \"npm:watch:rojo\" \"npm:server:rojo\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "chokidar-cli": "^3.0.0",
    "concurrently": "^9.2.1"
  }
}
